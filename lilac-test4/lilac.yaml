build_prefix: lilac-loong64

destsubdir: aur

post_build: git_pkgbuild_commit

repo_depends:
  - lilac-test1

update_on:
  - source: manual
    manual: 16

update_on_soname:
  - pkgname: lilac-test1
    libs: [ liblilactest.so ]